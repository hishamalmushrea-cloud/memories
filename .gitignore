# Gradle
.gradle/
build/
app/build/
**/build/
!gradle/wrapper/gradle-wrapper.jar

# Android Studio / IntelliJ
.idea/
*.iml
.kotlin/
captures/
.cxx/

# Local configuration and secrets -- never commit these
local.properties
*.keystore
*.jks
keystore.properties

# OS noise
.DS_Store
Thumbs.db

# Room's exported schema is generated by KSP. Commit it deliberately (it is
# review history for migrations); until then keep it out of the build verdict.
app/schemas/

# Signing reports / outputs that must not be tracked
*.apk.idsig
app/release/
/release/

# Python / tooling caches inside the repo
__pycache__/
.pytest_cache/
